const potato = 'potato';
let varr = 'hello';

console.log(varr);

const self = this;

function sum(num1, num2) {
  return num1 + num2;
}

function sayHello(firstName, lastName) {
  return `Hello ${firstName} ${lastName}, welcome to JS`;
};

let count = 0;

while (true) {
  count++;
  console.log(count);
  if (count === 51) {
    console.log(count);
    break;
  }
}

const myFunc = () => {
  this
};

console.log(sayHello('bryce', 'desbrisay'));

const array = new Promise((re));
